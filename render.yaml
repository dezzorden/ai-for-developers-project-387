services:
  - type: web
    name: call-booking
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /owner
    envVars:
      - key: PORT
        value: 10000
